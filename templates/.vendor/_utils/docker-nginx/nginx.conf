events {
}

http {
    server {
        listen 80;
        location /playground {
            root /usr/vendor;
            try_files $uri $uri/ index.html;
        }
        location ~ \.(js|css|zip) {
            root /usr/vendor/output;
            try_files $uri $uri/ =404;
        }
    }
}