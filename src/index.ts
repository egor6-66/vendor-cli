#! /usr/bin/env node

import path from 'path';
import yargs from 'yargs';
import { hideBin } from 'yargs/helpers';


// import { DevServer, GeneratePlugins, Init } from './services';

(() => {
    const configPath = path.resolve('');
    console.log(';eee')
    // yargs(hideBin(process.argv)).command(Init.command).command(GeneratePlugins.command).command(DevServer.command).demandCommand().help().argv;
})();
